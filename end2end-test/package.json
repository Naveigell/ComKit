{
  "scripts": {
    "test": "playwright test",
    "test:list": "playwright test --list",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:running-server": "PLAYWRIGHT_SKIP_WEB_SERVER=1 playwright test",
    "test:running-server:list": "PLAYWRIGHT_SKIP_WEB_SERVER=1 playwright test --list"
  },
  "dependencies": {
    "@playwright/test": "^1.58.2"
  }
}
